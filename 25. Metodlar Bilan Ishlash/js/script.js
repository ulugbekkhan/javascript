"use strict";


const box = document.querySelector("#box")
const buttons = document.querySelectorAll("button")
const circles = document.querySelectorAll(".circle")
const hearts = document.querySelectorAll(".heart")
const circleWrapper = document.querySelector(".circle__wrapper")



// box.style.backgroundColor = "red"
// box.style.width = "100px"
// box.style.height = "100px"

// box.style.cssText = "background-color: lime; width: 100px; height: 100px";

// buttons[0].style.width = "200px"
// circles[1].style.backgroundColor = "yellow"



// for (let i = 0; i < hearts.length; i++) {
//     hearts[i].style.backgroundColor = "blue"
// }

// hearts.forEach(item => {
//     item.style.backgroundColor = "gray"
// })



// const btn = document.createElement("button")
// document.body.append(btn) // append metodi element larni HTML ga qo'shadi



// const mycircle = document.createElement("div")
// mycircle.classList.add("circle") // add metodi class qo'shadi
// circleWrapper.append(mycircle)

// circleWrapper.before(mycircle)
// circleWrapper.after(mycircle)
// circles[0].before(mycircle)
// circles[0].after(mycircle)

// circles[1].remove()

// circles[1].replaceWith(mycircle)

// mycircle.innerHTML = "<pre>E</pre>"
// mycircle.innerHTML = "E"
// mycircle.textContent = "A"



// circleWrapper.insertAdjacentHTML(`beforebegin`, `<pre>E</pre>`)
// circleWrapper.insertAdjacentHTML(`beforeend`, `<pre>E</pre>`)
// circleWrapper.insertAdjacentHTML(`afterbegin`, `<pre>E</pre>`)
// circleWrapper.insertAdjacentHTML(`afterend`, `<pre>E</pre>`)